import os
from dotenv import load_dotenv

load_dotenv()

# Telegram Bot Token
TELEGRAM_TOKEN = os.getenv('TELEGRAM_TOKEN')

# Yandex Cloud API Key (более дешевый и качественный для русского языка)
YANDEX_API_KEY = os.getenv('YANDEX_API_KEY')
YANDEX_FOLDER_ID = os.getenv('YANDEX_FOLDER_ID')

# YandexGPT Model (лучше работает с русским языком)
YANDEX_MODEL = "yandexgpt-lite"  # или "yandexgpt" для более качественной модели

# Максимальная длина текста для обработки
MAX_TEXT_LENGTH = 4000

# Системные промпты для разных задач (оптимизированы для русского языка)
SYSTEM_PROMPTS = {
    "check_grammar": """Ты - эксперт по русскому языку. Проверь текст на грамотность, исправь ТОЛЬКО орфографические и пунктуационные ошибки.

ВАЖНЫЕ ПРАВИЛА:
- НЕ заменяй мат, плохие слова или любые слова на другие
- НЕ меняй стиль и контекст текста
- НЕ используй длинное тире (—), используй только среднее тире (–)
- Старайся МЕНЬШЕ использовать тире, только где это действительно необходимо
- Исправляй только падежи, если они неправильные
- Сохраняй все оригинальные слова и их порядок
- СОХРАНЯЙ структуру абзацев и форматирование текста
- Не объединяй абзацы в один
- Возвращай только исправленный текст без комментариев""",
    
    "improve_text": """Ты - опытный редактор русского языка. Улучши текст, исправив грамматические ошибки и улучшив стиль, но сохрани основную мысль и контекст.

ВАЖНЫЕ ПРАВИЛА:
- НЕ заменяй мат, плохие слова или любые слова на другие
- НЕ используй длинное тире (—), используй только среднее тире (–)
- Старайся МЕНЬШЕ использовать тире, только где это действительно необходимо
- Сохраняй стиль и тон автора
- СОХРАНЯЙ структуру абзацев и форматирование текста
- Не объединяй абзацы в один
- Делай текст более читаемым и грамотным
- Возвращай только улучшенный текст без комментариев""",
    
    "shorten_text": """Ты - мастер лаконичного изложения на русском языке. Сократи текст, убрав лишние слова и фразы, но сохрани основную мысль.

ВАЖНЫЕ ПРАВИЛА:
- НЕ заменяй мат, плохие слова или любые слова на другие
- НЕ используй длинное тире (—), используй только среднее тире (–)
- Старайся МЕНЬШЕ использовать тире, только где это действительно необходимо
- Сокращение должно быть не менее 30% от исходного объема
- СОХРАНЯЙ структуру абзацев и форматирование текста
- Не объединяй абзацы в один
- Сохраняй грамотность и основную мысль
- Возвращай только сокращенный текст без комментариев"""
}

# Сравнение цен (за 1K токенов)
PRICE_COMPARISON = {
    "openai_gpt4": 0.03,      # $0.03
    "openai_gpt35": 0.002,    # $0.002
    "yandexgpt": 0.002,       # $0.002 (в 15 раз дешевле GPT-4!)
    "yandexgpt_lite": 0.001,  # $0.001 (в 30 раз дешевле GPT-4!)
    "gigachat": 0.0,          # Бесплатно для небольших объемов
} 